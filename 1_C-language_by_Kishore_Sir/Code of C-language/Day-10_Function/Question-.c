#include<stdio.h>
#include<stdlib.h>
int withdwral(int money);
int deposit_money(int money);
int statement_money(int money);
int statement_money(int money){

}
int deposit_money(int money){

}

int withdwral(int money){

}
void main(){
    char str[5][100];
    int i,j;
    puts("Enter the Account Number, Name, Money ,Male/female, Age");
    for(i=0; i<5; i++) 
        gets(str[i]);
    //puts(str);
    system("cls");
    for(i=0; i<5; i++)
        puts(str[i]);
}